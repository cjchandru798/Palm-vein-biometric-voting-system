
  # Palm Vein Voting System Design

  This is a code bundle for Palm Vein Voting System Design. The original project is available at https://www.figma.com/design/rqzvt9YLOfAnUeFLt0RyUE/Palm-Vein-Voting-System-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  